<% @posts.each do |post| %>
	<h2><%= post.title %></h2>
	<div>Published: <%= post.created_at.strftime("%Y-%m-%d") %> at <%= post.created_at.strftime("%H:%M") %> EST
	<div><%= truncate(post.body, :length => 35) %></div>
	<br>
	<%= link_to "Read Post", post %>
<% end %>
